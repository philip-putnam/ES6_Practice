var React = require('react');

var Duplicate = React.createClass({

  render: function() {
    return (
      <div>
      <h1>Hello from duplicate-variables!</h1>
      </div>
    );
  }
});

module.exports = Duplicate;
